import { createUseStyles } from 'react-jss'

export const useStyles = createUseStyles(() => ({
  button: {
    background: '#1677FF',
    color: 'white',
    border: 'none',
    borderRadius: '8px',
    padding: '7px 11px',
    fontSize: '12px',
    fontWeight: 600,
    cursor: 'pointer',
  },
}))
