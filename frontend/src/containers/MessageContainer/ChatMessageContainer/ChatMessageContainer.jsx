import { useStyles } from './ChatMessageContainer.css'

const ChatMessageContainer = () => {
  const classes = useStyles()
  return <div className={classes.root}>ChatMessageContainer</div>
}

export default ChatMessageContainer
