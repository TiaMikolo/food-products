import { useStyles } from './RightMessageContainer.css'

const RightMessageContainer = () => {
  const classes = useStyles()
  return <div className={classes.root}>RightMessageContainer</div>
}

export default RightMessageContainer
